{
  "name": "web.api",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm install && knex migrate:rollback && knex migrate:latest && knex seed:run && nodemon server.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.1",
    "knex": "^0.19.5",
    "nodemailer": "^6.3.1",
    "nodemon": "^1.19.4",
    "pg": "^7.12.1"
  }
}
